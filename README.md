# java-almb-api

Reactive **Spring Boot OpenAPI Contract-First Demo** built with **Spring WebFlux**, **R2DBC**, and **OpenAPI Generator
**.

The project demonstrates how to generate REST API interfaces from an OpenAPI specification and implement them using a *
*reactive programming model**. 

---

# Project Overview

| Property    | Value           |
|-------------|-----------------|
| GroupId     | `org.almb`      |
| ArtifactId  | `java-almb-api` |
| Version     | `1.0-SNAPSHOT`  |
| Java        | 21              |
| Spring Boot | 3.2.5           |
| Build Tool  | Maven           |

---

# Technology Stack

* **Spring Boot 3**
* **Spring WebFlux (Reactive REST API)**
* **Spring Data R2DBC**
* **H2 Reactive Database**
* **OpenAPI Generator (Contract-First)**
* **SpringDoc OpenAPI / Swagger UI**
* **MapStruct**

---

# Architecture

The project follows a **contract-first approach** and **hexagonal architecture**:

1. Define API in an **OpenAPI YAML file**
2. Generate **controller interfaces and DTOs**
3. Implement the generated interfaces in the application

```
OpenAPI YAML
      │
      ▼
OpenAPI Generator
      │
      ▼
Generated Controllers + DTOs
      │
      ▼
Application Implementation
```

---

# OpenAPI Specification

Location:

```
src/main/resources/openapi/company-profitablity.yaml
```

Generated code:

```
target/generated-sources/openapi
```

Generated packages:

```
org.almb.adapter.in.web.controller
org.almb.adapter.in.web.dto
```

---
# Swagger UI

After starting the application:

```
http://localhost:8080/swagger-ui.html
```

This UI allows you to:

* Explore available endpoints
* Test APIs interactively
* View generated schema models

---
# Database

The project uses **H2 with R2DBC** for a reactive in-memory database.

Typical configuration example:

```
spring.r2dbc.url=r2dbc:h2:mem:///testdb
spring.r2dbc.username=sa
spring.r2dbc.password=
```

---

# Build the Project

```bash
mvn clean install
```

---

# Run the Application

```bash
mvn clean spring-boot:run
```

Application starts at:

```
http://localhost:8080

Swagger UI:
http://localhost:8080/swagger-ui/index.html

Health check:
http://localhost:8080/actuator/health

Metrics:
http://localhost:8080/actuator/metrics

Info:
http://localhost:8080/actuator/info
```

---

# Generated Source Integration

The **OpenAPI generator plugin** runs during the Maven build:

```
generate-sources phase
```

Generated sources are automatically added using:

```
build-helper-maven-plugin
```

---

# MapStruct

MapStruct is used for mapping between:

* Database entities
* DTOs
* API models

Annotation processing is enabled via the Maven Compiler Plugin.

---

# Testing

Testing uses:

* **JUnit Jupiter**
* **Testcontainers**

Run tests:

```bash
mvn test
```

---

# Project Structure

```
src
 ├── main
 │   ├── java/org/almb
 │   │    └── adapter
 │   │         └── in/web
 │   │              ├── controller
 │   │              └── dto
 │   └── resources
 │        └── openapi
 │             └── company-profitablity.yaml
 │
 └── test
```

---

# Maven Plugins

| Plugin                         | Purpose                                 |
|--------------------------------|-----------------------------------------|
| spring-boot-maven-plugin       | Build and run Spring Boot app           |
| openapi-generator-maven-plugin | Generate API interfaces and DTOs        |
| maven-compiler-plugin          | Compile Java with annotation processors |
| build-helper-maven-plugin      | Add generated sources to build          |

---

# Future Improvements

Possible enhancements:

* Enable **Spring Security**
* Add **PostgreSQL R2DBC**
* Implement **Docker support**
* Add **CI/CD pipeline**
* Add **integration tests with Testcontainers**

---

# License

This project is provided for demonstration and educational purposes.
Some parts in the project have been generated by ChatGPT


